// scripts/score/score.js
// Currently, base score is handled by main.js (time survived).
// This file will hold logic for coin collection and score multipliers in future steps.

function addScore(amount) {
    gameState.coinScore += amount;
}
